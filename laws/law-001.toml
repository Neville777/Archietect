# law-001 — part of Archietect's language specification.
# Laws are amended, not edited: a superseded law gets status = "superseded"
# and a supersedes pointer from its successor — semantics are versioned.

id = "law-001"
title = "Word-boundary matching, never substring"
statement = "A concept term matches a name only at snake_case/camelCase token boundaries with bounded inflection."
because = "'%sd%' matched USD and declared a broken pipeline healthy; 'story' claimed harvest_hiSTORY as a competing implementation, manufacturing a fake architectural problem."
discovered_in = ["TITAN", "local-sweep"]
introduced = "2026-08-05"
status = "active"
category = "parser"
# parser — this is a scanner correctness rule, not architectural philosophy.
# The matching logic lives in model::names_concept / model::same_word.
# The regression fixture prevents the substring bug from recurring; the
# rule itself belongs with the tokeniser, not the law registry.
regression = "tests/laws.rs::law_001 (fixture: tests/fixtures/law_001)"
