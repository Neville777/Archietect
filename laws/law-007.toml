# law-007 — part of Architect's language specification.
# Laws are amended, not edited: a superseded law gets status = "superseded"
# and a supersedes pointer from its successor — semantics are versioned.

id = "law-007"
title = "ORM declarations outrank SQL-string-only concepts on exact ties"
statement = "When two exact-name candidates tie, the one with a non-SQL (ORM/schema) declaration wins."
because = "redash: a phantom lowercase `query` outranked the real Query model on usage inflated by every FROM query in the repo. A model class is a strong declaration; text that happens to say CREATE TABLE is the weakest."
discovered_in = ["redash"]
introduced = "2026-08-05"
status = "active"
category = "ranking"
# ranking — this principle is now encoded as scoring::ORM_DECLARED = 1_000
# in src/scoring.rs, which structurally outranks any usage+relation score
# on an exact-name tie. The regression fixture stays; the constant IS the
# rule. Future ranking wrong answers tune a constant, not a new law.
regression = "tests/laws.rs::law_007 (fixture: tests/fixtures/law_007)"
