/// A pre-existing, unrelated exchange connector — nothing to do with
/// experiment dispatch. Minimal reproduction of TITAN's real
/// `crates/titan_live_connector/src/executor.rs`.
pub struct BinanceExecutor {
    pub api_key: String,
}
